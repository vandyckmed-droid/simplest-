{
  "name": "ranks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Volatility-adjusted momentum ranking over every tradeable US stock, with HRP basket weighting",
  "scripts": {
    "etf:check": "node src/etf-check.js",
    "build": "node src/ranks-build.js && node src/etf-build.js && node src/ranks-render.js",
    "data": "node src/ranks-build.js",
    "etfs": "node src/etf-build.js",
    "render": "node src/ranks-render.js"
  },
  "engines": {
    "node": ">=20"
  }
}
